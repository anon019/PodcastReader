# Podcast Reader 产品与运行契约

版本：0.4（个人本地版）

## 1. 产品定义

Podcast Reader 是一个本地 macOS 内容阅读器，职责只有三件事：

1. 同步订阅的 YouTube Podcast；
2. 保存完整 Transcript 与简体中文翻译；
3. 把长内容整理成可连续阅读的结构化编辑稿。

它不是笔记工具，不提供自由写作、笔记本、学习卡或 Prompt 管理。用户只需要管理订阅、检查更新、标记已读与阅读内容。

## 2. 运行方式

- App 平时不常驻，只在用户打开时读取本地 SQLite。
- 不安装 LaunchAgent、Login Item、菜单栏 Helper、本地 API 或常驻 Python 服务。
- Codex automation 每天 07:30 在项目目录运行一次 `scripts/daily_update.sh`，完成后退出。
- App 的“检查更新”执行同一套一次性 pipeline，完成后退出子进程。
- 计划任务使用 `gpt-5.6-luna`，automation reasoning effort 为 `max`；内容子任务若不支持目标推理参数则使用 Luna 默认值，不切换到 Sol。
- Hermes 的任务、21 个频道配置与运行状态完全不修改；本项目只借鉴来源选择思路，数据和任务均独立。

## 3. 来源与更新

- 首版内置 21 个来源，每个来源包含独立的节目定位、主持/嘉宾核验要求、信息提炼重点、证据要求和降权规则。
- 支持新增 YouTube 频道、播放列表和手动单集链接。
- 新增频道或列表时，Luna 自动研究节目并生成专属 Source Profile；该过程不要求用户选择模板或编辑 Prompt。
- 每次更新检查最新长节目，按来源最低时长过滤 Shorts 和片段。
- 日常检查采用 30 天回看窗口，但只接收晚于该来源本地最新发布时间水位线的节目，每个来源单次最多新增 5 期；主键再以 YouTube video ID 去重。因此重复检查不会产生重复单集，也不会把未知的旧节目持续倒灌成“增量”。
- `failed` 与 `no_transcript` 会在后续每日任务继续重试；来源网络错误会写入本次运行的失败数，并在来源管理界面显示具体原因。
- 若 RSS 被 Shorts 挤满，回退到频道 Videos 页查找最近完整节目。
- 最新一集没有 Transcript 时保留条目并标记原因；可使用最近一集有完整字幕的节目作为可读样本，但不能把旧节目伪装成最新一集。

## 4. Transcript 契约

- 仅获取 YouTube 已提供的 Transcript，不下载音频，不做本地 ASR。
- 保存完整原文、语言、时间戳、段落顺序和中文翻译。
- 对长节目执行字幕密度检查，拒绝把视频描述或章节摘要当成完整 Transcript。
- 翻译逐段执行并可续跑，只补齐缺失段落，不改写原文。
- 阅读器支持只看原文、只看中文、左右对照三种模式。

## 5. 结构化提炼契约

每期必须输出：

- 一句话读懂；
- 实际主持人、共同主持人和嘉宾的背景、参与关系与核验链接；
- 按语义组织的话题地图；
- 核心洞察、关键数字、条件、影响和时间戳；
- 值得继续追踪的引申内容；
- 证据与限制；
- 下一步验证问题。

Transcript 是本期观点的唯一主证据。外部搜索只用于核验人物和机构背景，不能补写节目没有表达的观点。

输出采用自然的媒体编辑稿写法。正文禁止反复出现“字幕转述”“主持人判断”“我的归纳”等分析过程标签；事实、观点和推断的边界通过 speaker、时间戳、confidence 与独立的“证据与限制”表达。

## 6. 阅读体验

- 三栏结构：内容书架、单集列表、阅读正文。
- 支持跟随系统、浅色、深色三种外观并记住选择；两套配色均以正文对比度优先，避免白底白字或暗底灰字。
- 导航使用 macOS 系统字体；中文长文使用 PingFang SC，核心结论使用 Songti SC，英文原文使用系统 serif 设计。
- 默认正文 17 pt，用户可在 15–22 pt 之间即时调节。
- 高分辨率封面按 `maxresdefault → sddefault → hqdefault` 回退，避免放大低分辨率缩略图。
- 结构化页采用结论卡、参与者卡、话题卡和编号洞察，减少连续密集文本。
- 每个列表条目和阅读页都醒目显示 YouTube 原始发布日期与本地整理完成日期；翻译重试、已读状态或后续打开 App 不得修改整理日期。
- 只保留已读/未读、原节目链接、更新、添加来源等必要操作。

## 7. 本地数据与状态

SQLite 是 App 与一次性任务的唯一事实源，保存：

- Source 与自动生成的 Source Profile；
- Episode、YouTube 原始发布时间、独立的本地整理完成时间、封面、状态和失败原因；
- Transcript 原文段落与译文；
- 结构化分析 JSON；
- 已读状态与更新运行记录。

状态至少包括 `discovered`、`transcript_fetching`、`no_transcript`、`analyzing`、`complete` 和 `failed`。失败必须保留可读原因，不能静默消失。

## 8. MVP 验收

- 21 个默认来源全部存在独立 Profile。
- 每个来源至少有一个完成结构化提炼的可读样本。
- 这 21 个样本的 Transcript 原文与翻译完整落库。
- 最新节目无字幕时有明确 UI 标记与原因。
- 手动更新和每日自动更新均为一次性执行，结束后没有项目进程常驻。
- App 图标包含透明边缘和标准 macOS `.icns` 尺寸，不显示生成图白色画布。
- 浅色与深色阅读界面的正文、时间、状态、卡片边界均清晰可读，外观选择在重启后保持。
